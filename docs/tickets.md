# Tickets: Friensyashki Messenger

## Правило версионирования

- новая функциональность задачи -> bump patch (`v0.1.0` -> `v0.1.1`);
- если задача существенно расширяет API/контракты, допускаем bump minor.

## Этап 0: проектный каркас

1. **TASK-001 | Версия `v0.1.0`** ✅
   - Что делаем: создать каркас `messenger`-сервиса (структура, конфиг, health endpoint, docker-compose integration).
   - Тесты:
     - `GET /health` -> `200`;
     - сервис поднимается в docker без ошибок.

2. **TASK-002 | Версия `v0.1.1`** ✅
   - Что делаем: подключить PostgreSQL + Alembic, создать базовые миграции (`dialogs`, `dialog_participants`, `messages`, `message_statuses`).
   - Тесты:
     - миграции применяются на чистой БД;
     - повторный прогон миграций не ломается;
     - базовые CRUD smoke-tests репозиториев.

## Этап 1: интеграция с auth

3. **TASK-003 | Версия `v0.1.2`** ✅
   - Что делаем: middleware/dependency для идентификации пользователя через `auth` (через gRPC `GetUserInfoByToken`).
   - Тесты:
     - валидный токен -> доступ к защищенному endpoint;
     - невалидный/просроченный/blacklist токен -> `403`.

## Этап 2: диалоги 1:1

4. **TASK-004 | Версия `v0.1.3`** ✅
   - Что делаем: `POST /dialogs/direct` (создание/получение 1:1 диалога по `target_login`).
   - Тесты:
     - первый вызов создаёт диалог;
     - повторный вызов с той же парой возвращает существующий;
     - пользователь не может создать диалог с самим собой;
     - несуществующий `target_login` возвращает корректную ошибку.

5. **TASK-005 | Версия `v0.1.4`** ✅
   - Что делаем: `GET /dialogs` со списком диалогов пользователя.
   - Тесты:
     - пользователь видит только свои диалоги;
     - сортировка по последней активности корректна.

## Этап 3: текстовые сообщения

6. **TASK-006 | Версия `v0.1.5`** ✅
   - Что делаем: `POST /dialogs/{id}/messages` (сохранение текста + идемпотентность по `client_message_id`).
   - Тесты:
     - сообщение сохраняется и возвращается с `id`, `created_at`;
     - повтор с тем же `client_message_id` не создаёт дубликат;
     - отправка в чужой диалог запрещена.

7. **TASK-007 | Версия `v0.1.6`** ✅
   - Что делаем: `GET /dialogs/{id}/messages` (история с пагинацией).
   - Тесты:
     - корректные `limit/offset`;
     - порядок сообщений детерминированный;
     - доступ только участникам диалога.

## Этап 3.5: минимальный UI для ручного тестирования

8. **TASK-UI-001 | После `v0.1.6`** ✅
   - Что делаем: минимальный веб-клиент (HTML/JS) для ручной проверки сценариев: логин через `auth`, список диалогов, создание диалога, отправка и просмотр сообщений. Позднее дополняется WebSocket-событиями из Этапа 4.
   - Тесты:
     - клиент позволяет авторизоваться и увидеть список диалогов ✅;
     - пользователь может создать диалог и отправить сообщение ✅;
     - история сообщений отображается корректно ✅.

## Этап 4: realtime и статусы

9. **TASK-008 | Версия `v0.1.7`** ✅
   - Что делаем: WebSocket подключение пользователя, получение новых сообщений online.
   - Тесты:
     - два клиента в диалоге получают событие `new_message`;
     - после reconnect клиент продолжает получать события.

10. **TASK-009 | Версия `v0.1.8`** ✅
   - Что делаем: статус `delivered` при доставке в активную сессию.
   - Тесты:
     - при активном WS у получателя статус обновляется на `delivered`;
     - статусы не проставляются неучастникам диалога.

10.1. **TASK-009a | После `v0.1.8`** ✅ — автообновление списка диалогов
    - Что делаем: убрать кнопку «Обновить диалоги»; при получении события `new_message` по WebSocket вызывать обновление списка диалогов (`loadDialogs()`), чтобы порядок по последней активности и список обновлялись без ручного действия.
    - Тесты:
      - кнопки «Обновить диалоги» в UI нет;
      - при приходе нового сообщения (в текущем или другом диалоге) список диалогов в сайдбаре обновляется автоматически.

11. **TASK-010 | Версия `v0.1.9`** ✅
    - Что делаем: `POST /messages/{id}/read` и статус `read`.
    - Тесты:
      - участник может отметить сообщение прочитанным ✅;
      - повторный вызов идемпотентен ✅;
      - отправитель видит обновление статуса ✅.

12. **TASK-011 | Ошибки при регистрации дублируются** ✅
    - Что делаем: исправить дублирование сообщений об ошибках в форме регистрации.
    - Поведение: при неверном email ошибка показывается один раз; при несоответствии пароля требованиям одна и та же ошибка показывается дважды.
    - Примеры со скриншотов:
      - Email: при вводе «vjk» — одна ошибка: «value is not a valid email address: An email address must have an @-sign.»
      - Пароль: при коротком пароле — дублирование: «Password must be at least 8 characters • Password must be at least 8 characters»
    - Причина: оба поля `password` и `confirm_password` используют один тип `PasswordField` с одинаковыми валидаторами; API возвращает ошибки для каждого поля, фронтенд склеивал все без дедупликации.
    - Решение: `formatApiError()` в `web/index.html` — дедупликация через `new Set` перед `join(' • ')`.
    - Тесты:
      - каждое сообщение об ошибке валидации отображается один раз ✅;
      - для email и для пароля нет дублирования текста ошибки ✅.

## Этап 5: стабилизация MVP

13. **TASK-012 | Версия `v0.2.0`**
    - Что делаем: observability (логи/метрики), лимиты, базовые перф-тесты, документация.
    - Тесты:
      - smoke e2e: login -> create dialog -> send -> receive -> read;
      - регрессионный прогон API + WS;
      - нагрузочный тест на целевую MVP-нагрузку.

14. **TASK-013 | Версия `v0.2.0` (расширение)**
    - Что делаем: мониторинг через Grafana (дашборды, алерты). Набор метрик — согласовать отдельно; минимум: количество зарегистрированных пользователей.
    - Тесты:
      - метрики экспортируются и доступны в Grafana;
      - дашборд с числом зарегистрированных пользователей отображает актуальные данные.

15. **TASK-014 | Версия `v0.2.0` (расширение)**
    - Что делаем: провести нагрузочное тестирование (сценарии, целевые RPS/латентности — согласовать; зафиксировать результаты и базу для регрессий).
    - Тесты:
      - нагрузочные сценарии пройдены, результаты зафиксированы;
      - выявленные узкие места отражены в отчёте/документации.

## Backlog после стабилизации v0.2.x

- **TASK-015 | Версия `v0.2.1` (план)**
  - Что делаем: добавить `long-polling fallback` для realtime при недоступности WebSocket.
  - Тесты:
    - при штатном состоянии используется WebSocket;
    - при недоступном WebSocket клиент переключается на long-polling;
    - доставка сообщений сохраняется без потери событий.

- **TASK-SEC-001 | Версия `v0.2.2` (план)**
  - Что делаем: настроить SSL для PostgreSQL и добавить `sslmode=require` в DSN обоих сервисов (`auth`, `messenger`). В production — сертификаты для PG; в docker-dev — опционально.
  - Тесты:
    - подключение к PostgreSQL с `sslmode=require` проходит без ошибок;
    - подключение без SSL отклоняется сервером (production).

- **TASK-SEC-002 | Версия `v0.2.3` (план)**
  - Что делаем: привести хранение и обработку персональных данных в соответствие с законодательством РФ (152-ФЗ и сопутствующие НПА) и подготовить детальный план изменений в backend и UI (чекбоксы согласия, ссылки на политику и т.п.).
  - Подзадачи:
    - изучить актуальные требования законодательства РФ о персональных данных (152-ФЗ, подзаконные акты, рекомендации Роскомнадзора), применительно к функциональности мессенджера;
    - подготовить отдельный файл в `docs/` с краткой выжимкой ключевых требований и терминов (какие ПД мы обрабатываем, роли оператора/обработчика, правовые основания, сроки хранения и т.д.);
    - на основе выжимки сформировать подробный чек-лист изменений по backend (соглашения/политика, аудит и логирование операций с ПД, маскирование/удаление, экспорт/удаление аккаунта и т.п.);
    - сформировать чек-лист изменений по frontend/UI (чекбоксы согласия при регистрации и в настройках, ссылки на политику ПД/пользовательское соглашение, отдельные настройки приватности и т.д.) и задокументировать ожидаемое поведение.
  - Тесты:
    - файл с выжимкой требований по ПД в РФ добавлен в `docs/` и согласован;
    - чек-листы по backend и frontend утверждены и разбиты на отдельные тикеты/подзадачи в `tickets.md` или issue-трекере;
    - в UI определён минимальный обязательный набор элементов согласия с обработкой ПД (чекбоксы, ссылки на политику), поведение соответствует сформированным требованиям.

- **TASK-016 | Версия `v0.2.4` (план)**
  - Что делаем: добавить пользовательские аватарки (upload/update/get) с хранением в S3-совместимом хранилище (`prod` — managed S3, `local` — MinIO).
  - Тесты:
    - пользователь может загрузить аватар допустимого формата и размера;
    - обновление аватарки заменяет ссылку на предыдущую;
    - получение профиля возвращает актуальный URL/ключ аватарки;
    - при невалидном формате или превышении лимита возвращается корректная ошибка.

- **TASK-017 | Версия `v0.2.5` (план)**
  - Что делаем: возможность смены логина пользователем. После смены логина сервер auth выдаёт новый JWT с новым логином; клиент обязан обновить токен (refresh), чтобы в UI сразу отображался новый логин без лага, когда подсасывается старый логин из старого токена.
  - Тесты:
    - успешная смена логина возвращает новый access (и при необходимости refresh) токен с новым `login` в payload;
    - клиент после смены логина использует новый токен; в UI отображается новый логин без промежуточного отображения старого;
    - валидация логина (уникальность, формат); корректные ошибки при конфликте или невалидном значении.

- **TASK-018 | Версия `v0.2.6` (план)**
  - Что делаем: ввести систему ролей пользователей (как минимум `user`, `admin`) и обеспечить возможность безопасного создания/назначения администратора (через миграцию и/или отдельный управляемый flow).
  - Подзадачи:
    - добавить в доменную модель пользователя поле/таблицу для ролей;
    - продумать и реализовать безопасный процесс первоначального создания администратора (через миграцию, CLI-команду или отдельный защищённый endpoint);
    - описать, какие действия в API и UI требуют роли `admin`, и добавить проверки;
    - обновить документацию по auth/messenger с учётом ролей.
  - Тесты:
    - по умолчанию новые пользователи получают только роль `user`;
    - администратор может быть создан только через контролируемый канал (миграция/CLI/ограниченный endpoint);
    - попытки выполнить админские действия без роли `admin` возвращают корректную ошибку авторизации.

- **TASK-UI-003 | Версия `v0.2.7` (план, завершение MVP UI)**
  - Что делаем: привести frontend к «нормальной» структуре (разбиение по папкам и файлам, базовая дизайн-система) и реализовать отдельную админ-панель на фронтенде.
  - Подзадачи:
    - реорганизовать текущий фронтенд: выделить модули/страницы, компоненты, хуки/сервисы API, конфиги, стили;
    - описать и внедрить минимальный набор общих UI-компонентов (кнопки, инпуты, формы, layout), договориться о структуре директорий;
    - реализовать админ-панель (отдельный раздел/route), доступную только пользователям с ролью `admin`, с базовыми действиями (например: просмотр списка пользователей/диалогов, техническая информация, дальше расширяем);
    - обновить документацию по frontend-архитектуре и зафиксировать принятый стиль/паттерны.
  - Тесты:
    - сборка/линтер/тесты frontend проходят после реорганизации структуры;
    - пользователи без роли `admin` не имеют доступа к админ-панели;
    - навигация между пользовательской частью и админ-панелью работает предсказуемо и соответствует описанной архитектуре.

- **TASK-019 | Версия `v0.2.8` (план)**
  - Что делаем: гарантировать уникальность логинов (юзернеймов) и запретить создание пользователя с логином, который уже существует в системе (в том числе с учётом пользователей, уже заведённых в мессенджере/auth).
  - Подзадачи:
    - убедиться, что на уровне БД и доменной модели есть уникальный индекс/ограничение по полю логина;
    - синхронизировать поведение auth и messenger так, чтобы не было расхождения в правилах уникальности;
    - определить и реализовать формат ошибки при попытке создать пользователя с занятым логином (HTTP-статус, код/текст ошибки);
    - обновить frontend-валидацию/обработку ошибок, чтобы пользователь видел понятное сообщение при попытке регистрации/смены логина на уже занятый.
  - Тесты:
    - попытка зарегистрировать пользователя с логином, который уже существует, завершается предсказуемой ошибкой без создания дубля;
    - при одновременных конкурирующих запросах на один и тот же логин нет «проскальзывания» дублей;
    - UI корректно отображает и обрабатывает ошибку занятости логина.

- **TASK-UI-004 | Версия `v0.2.9` (план)**
  - Что делаем: добавить визуальную маркировку администраторов в UI — рядом с ником/логином админа отображать заметный золотой лейбл «admin», видимый всем пользователям.
  - Подзадачи:
    - определить, в каких местах интерфейса должен отображаться статус админа (список диалогов, сообщения, профиль и т.д.) и зафиксировать это в документации;
    - реализовать компонент/бейдж «admin» (золотой цвет, понятная читаемость, доступность), завязанный на роли пользователя;
    - интегрировать бейдж в нужные UI-экраны так, чтобы он отображался для всех, кто видит ник/логин администратора;
    - продумать адаптивность/тёмную тему, чтобы «золотой» лейбл везде выглядел нормально.
  - Тесты:
    - у пользователей с ролью `admin` рядом с ником/логином в оговорённых местах UI показывается золотой лейбл «админ»;
    - у пользователей без роли `admin` никакой админ-маркировки нет;
    - лейбл корректно отображается на разных устройствах и в разных цветовых темах.

- **TASK-020 | Версия `v0.2.10` (план)**
  - Что делаем: реализовать реальное подтверждение электронной почты пользователей через одноразовый код, отправляемый на email, и интегрировать это в регистрацию/управление аккаунтом.
  - Подзадачи:
    - продумать и задокументировать общий процесс подтверждения email (когда отправляем код, срок жизни, ограничения по попыткам, можно ли менять email и как происходит повторная верификация);
    - добавить в доменную модель пользователя поля для состояния верификации email (подтверждён/неподтверждён, дата верификации и т.п.);
    - реализовать генерацию одноразового кода, хранение/валидацию и ограничение по времени/количеству попыток (в БД и/или Redis);
    - интегрировать отправку кода через выбранный email-провайдер (или заглушку в dev-режиме), задокументировать формат писем;
    - добавить API-эндпоинты/методы: запрос кода подтверждения, подтверждение кода, повторная отправка кода с защитой от спама;
    - обновить frontend: формы/экраны для ввода кода, статусы «email неподтверждён/подтверждён», сценарии повторной отправки;
    - определить, какие действия в системе запрещены/ограничены до подтверждения email, и реализовать соответствующие проверки.
  - Тесты:
    - пользователь при регистрации/смене email получает одноразовый код на почту и может успешно подтвердить email в пределах заданного времени;
    - неверный или просроченный код приводит к предсказуемой ошибке без изменения состояния пользователя;
    - ограничения по количеству попыток и повторным отправкам кода работают корректно и защищают от брутфорса/спама;
    - UI корректно проводит пользователя через сценарий подтверждения и показывает понятные сообщения об ошибках/успехе.

- **TASK-021 | Версия `v0.2.11` (план)**
  - Что делаем: добавить поддержку групповых чатов (многопользовательские диалоги с именем/аватаркой группы и управлением участниками).
  - Подзадачи:
    - спроектировать и добавить в модель данных сущности для группового диалога (таблица/модель группы, роли участника в группе: владелец, админ, участник);
    - реализовать API для создания группы, приглашения/добавления и удаления участников, выхода из группы, изменения названия/аватарки;
    - определить и реализовать модель прав внутри группы (кто может переименовывать, удалять участников, назначать админов и т.д.);
    - адаптировать модель сообщений/статусов под групповые чаты (отправитель, список получателей, статусы доставки/прочтения по участникам);
    - обновить UI: список групповых чатов, экран группы, управление участниками, различимая индикация «группового» диалога;
    - обновить документацию API/архитектуры для групп.
  - Тесты:
    - пользователь может создать групповой чат, добавить и удалить участников в рамках разрешённых прав;
    - сообщения в группе доставляются всем участникам, статусы отображаются корректно;
    - операции управления группой (переименование, смена аватарки, назначение админов) доступны только тем ролям, которым это разрешено.

- **TASK-022 | Версия `v0.2.12` (план)**
  - Что делаем: добавить возможность отправлять в сообщениях произвольные файлы (изображения, аудио, видео, документы: PDF, Word, Excel и т.п.) с безопасным хранением и скачиванием/просмотром.
  - Подзадачи:
    - спроектировать модель хранения вложений (метаданные файла, тип, размер, ссылка/ключ в хранилище) и связать её с сообщениями;
    - выбрать и интегрировать файловое хранилище (S3-совместимое, MinIO в dev) с лимитами по размеру/типам и политиками доступа;
    - реализовать API/эндпоинты для загрузки файлов, привязки к сообщению, получения/скачивания и, при необходимости, удаления/истечения срока жизни;
    - добавить валидацию типов и размеров файлов, защиту от загрузки потенциально опасного контента (белый список/ограничения);
    - обновить UI для отправки и отображения вложений (превью для картинок/аудио, иконки/кнопки скачивания для документов);
    - продумать и задокументировать политику хранения/удаления файлов (сроки, очистка, бэкапы).
  - Тесты:
    - пользователь может отправить допустимый файл, и он корректно прикрепляется к сообщению и доступен получателям;
    - попытка загрузить файл недопустимого типа или превышающего лимит размера приводит к предсказуемой ошибке без сохранения;
    - доступ к скачиванию/просмотру файлов есть только у участников соответствующего диалога/группы;
    - UI корректно отображает разные типы вложений и поведение стабильно на разных устройствах.

- **TASK-UI-002 | После `v0.2.5` (план)**
  - Что делаем: UI для смены логина (форма/модалка в настройках профиля): поле нового логина, подтверждение, вызов API смены логина, обновление токена и отображения логина в приложении без перезагрузки.
  - Тесты:
    - пользователь может открыть форму смены логина и отправить запрос;
    - после успешной смены логин в шапке/профиле обновляется без перезагрузки страницы;
    - при ошибке (занятый логин, невалидный формат) отображается сообщение об ошибке.
